services:
  okonomi:
    build: .
    image: personlig-okonomi:latest
    container_name: personlig-okonomi
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
